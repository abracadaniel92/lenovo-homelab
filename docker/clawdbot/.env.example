# Clawdbot Gateway Configuration
# Security: Gateway binds to loopback only (127.0.0.1) - not exposed externally
# Copy this file to .env and fill in the values

# Gateway settings
CLAWDBOT_GATEWAY_BIND=127.0.0.1
CLAWDBOT_GATEWAY_PORT=18789
# Generate a secure token: openssl rand -hex 32
CLAWDBOT_GATEWAY_TOKEN=

# Mattermost Integration
# Get bot token from Mattermost: System Console > Integrations > Bot Accounts
# Base URL should be the internal Docker network URL
MATTERMOST_BOT_TOKEN=
MATTERMOST_URL=http://mattermost:8065

# Optional: Claude API (if using cloud models instead of local Ollama)
# CLAUDE_AI_SESSION_KEY=
# CLAUDE_WEB_SESSION_KEY=
# CLAUDE_WEB_COOKIE=

# Optional: Ollama Configuration (if using local models)
# OLLAMA_HOST=http://ollama:11434
